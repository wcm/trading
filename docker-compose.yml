services:
  trading-bot:
    build: .
    image: trading-bot:paper
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./config/settings.yaml:/app/config/settings.yaml:ro
      - ./data:/app/data
      - ./logs:/app/logs
    stop_grace_period: 30s
